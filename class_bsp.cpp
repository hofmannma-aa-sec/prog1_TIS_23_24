class class
{
private:
    /*
    Dies ist ein Zugriffsmodifizierer, 
    der angibt, 
    dass die nachfolgenden Mitglieder der Klasse nur innerhalb der Methoden dieser Klasse zugänglich sind.
    */
   
    /* data */
public:
    // Konstruktor call
    class(/* args */);
    // Destruktor call
    ~class();
};
/*
Dies ist der Konstruktor der Klasse.
Ein Konstruktor ist eine spezielle Methode,
die aufgerufen wird,
wenn ein Objekt dieser Klasse erstellt wird.
/* args */ ist ein Platzhalter für eventuelle Parameter.
*/
class::class(/* args */)
{
}
/*
Dies ist der Destruktor der Klasse. 
Ein Destruktor ist eine spezielle Methode,
die aufgerufen wird,
wenn ein Objekt dieser Klasse zerstört wird.
*/
class::~class()
{
}
